import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    cardsContainer: {
        marginTop: 16,
        gap: 25,
        padding: 16,
      },
});

export default styles;